# Spring-Boot-RestFull-CRUD-API

This is a Restful CRUD API for a simple user management application. First I built the APIs to create, retrieve, update, and delete a user and then tested them using Postman
